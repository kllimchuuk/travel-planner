APP_NAME="Travel Planner API"
DEBUG=True

# SQLite (default, no extra dependencies needed)
DATABASE_URL=sqlite:///./travel_planner.db

# PostgreSQL (used with Docker, requires psycopg2-binary)
# DATABASE_URL=postgresql://travel_user:travel_pass@localhost:5432/travel_planner_db

ART_INSTITUTE_API_URL=https://api.artic.edu/api/v1

BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=secret
